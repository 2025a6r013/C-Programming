#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int R, C;
    scanf("%d %d", &R, &C);
    int matrix[R][C], transpose[C][R];
    for (int i = 0; i < R; i++) {
        for (int j = 0; j < C; j++) {
            scanf("%d", &matrix[i][j]);
        }
    }
    for (int i = 0; i < R; i++) {
        for (int j = 0; j < C; j++) {
            transpose[j][i] = matrix[i][j];  
        }
    }
    for (int i = 0; i < C; i++) {
        for (int j = 0; j < R; j++) {
            printf("%d ", transpose[i][j]);
        }
        printf("\n");
    }
    return 0;
}
