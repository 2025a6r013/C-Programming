#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    long long first = 0, second = 1, next;
    scanf("%d", &N);
    for (int i = 0; i < N; i++) {
        if (i == 0) {
            printf("%lld", first);
        } else if (i == 1) {
            printf(" %lld", second);
        } else {
            next = first + second;
            printf(" %lld", next);
            first = second;
            second = next;
        }
    }
    return 0;
}
