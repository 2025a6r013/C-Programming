#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    float P, R, CI;
    int T;
    scanf("%f %f %d", &P, &R, &T);
    CI = P * (pow((1 + R/100), T) - 1);
    printf("%.2f\n", CI);
    return 0;
}
